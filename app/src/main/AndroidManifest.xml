<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dreamproperty">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DreamProperty">
        <activity android:name=".buyProperty.ShowDetailsOfFarmProperty"></activity>
        <activity android:name=".addProperty.AddFarmPropertiesLastPage" />
        <activity android:name=".addProperty.AddFarmProperties" />
        <activity android:name=".buyProperty.ShowDetailsOfOfficeProperty" />
        <activity android:name=".sellProperty.SellProperty" />
        <activity android:name=".addProperty.AddofficeProperties" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".buyProperty.BuyProperty"
            android:label="@string/title_activity_buy_property" />
        <activity android:name=".buyProperty.ShowDetailsOfHouseproperty" />
        <activity android:name=".buyProperty.SimplelearnFirestore" />
        <activity android:name=".buyProperty.Buyproperties" />
        <activity android:name=".addProperty.AddpropertiesLastPage" />
        <activity android:name=".addProperty.AddPropertiesNext" />
        <activity android:name=".addProperty.AddProperties" />
        <activity android:name=".Dashboard" />
        <activity android:name=".EditUserProfile" />
        <activity android:name=".login.UserLogin" />
        <activity android:name=".registerNewUser.registerNewUser" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <uses-library
            android:name="org.apache.http.legacy"
            android:required="false" />
    </application>

</manifest>